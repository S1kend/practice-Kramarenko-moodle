1. Открываем файл "docker-compose.yml" в VS Code, открываем там терминал Git Bash и пишем: docker-compose up -d
2. Ждём пока контейнеры поднимутся и в любом браузере переходим по ссылке http://localhost:8080
